{
  "private": true,
  "name": "@portkey/example",
  "version": "2.0.0-alpha.23",
  "scripts": {
    "start": "cross-env craco start",
    "pin": "sh -c '{ for X in {core,empty}; do lerna add @web3-react/$X --exact --no-bootstrap --scope=example-cra; done }'"
  },
  "dependencies": {
    "@portkey/did-ui-react": "^2.0.0-alpha.23",
    "@types/react-dom": "^18.0.0",
    "aelf-sdk": "^3.2.45-beta.0",
    "react-dom": "^18.0.0",
    "react-router-dom": "^6.4.0",
    "react-scripts": "^5.0.1"
  },
  "devDependencies": {
    "@craco/craco": "^6.4.2",
    "cross-env": "^7.0.3",
    "ts-loader": "^9.4.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
